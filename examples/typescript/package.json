{
  "name": "dtunnel-sdk-example-typescript",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:webview": "npm run build && node ../../scripts/build-html.mjs --dist dist --out webview/index.html",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "dtunnel-sdk": "^1.1.0"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^6.2.0"
  }
}
